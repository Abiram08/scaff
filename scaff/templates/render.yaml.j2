services:
  - type: web
    name: {{ agent_name }}
    env: docker
    dockerfilePath: ./Dockerfile
    plan: free
    {% if ui_mode == "fastapi" %}
    envVars:
      - key: PORT
        value: "8000"
    {% endif %}
